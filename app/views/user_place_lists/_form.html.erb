<%= form_for(@user_place_list) do |f| %>
  <% if @user_place_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_place_list.errors.count, "error") %> prohibited this user_place_list from being saved:</h2>

      <ul>
      <% @user_place_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
